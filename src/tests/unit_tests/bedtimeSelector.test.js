import { render } from "@testing-library/react";
import BedtimeSelector from "../../components/bedtimeSelector";

describe('the bedtime selector', () => {
    it('should render', () => {
        render(<BedtimeSelector />);
    });
    }
);
